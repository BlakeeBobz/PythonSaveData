from setuptools import setup, find_packages

setup(
    name='pythonsavedata',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # list dependencies here, e.g. 'requests',
    ],
    author='BlakeeBobz',
    description='Simple way to save data in python',
    url='https://github.com/BlakeeBobz/PythonSaveData',
)
